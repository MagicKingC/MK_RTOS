{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build and debug run",
            "command": "rm -rf ${workspaceFolder}/build/; mkdir ${workspaceFolder}/build/; cd ${workspaceFolder}/build/; cmake .. ; make; make debug",
            "args": [

            ]
        },
        {
            "type": "shell",
            "label": "build and run",
            "command": "rm -rf ${workspaceFolder}/build/; mkdir ${workspaceFolder}/build/; cd ${workspaceFolder}/build/; cmake .. ; make; make run",
            "args": [

            ]
        },
        {
            "type": "shell",
            "label": "stop",
            "command": "killall",
            "args": [
                "qemu-system-arm"
            ]
        }
    ]
}